services:
  gateway:
    build: .
    container_name: llm-gateway
    ports:
      - "8080:8080"
    volumes:
      - gateway-data:/data
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true

volumes:
  gateway-data:
